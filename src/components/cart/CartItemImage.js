import { useState } from "react";

function CartItemImage (cart) {

    return(
         <div className="cart__box">
            <img src={x.img} alt={x.alt} className="cart__img" />
        </div>
    )
}